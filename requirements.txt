# Temel — İP2/İP3 için bunlar yeterli
numpy>=1.26
PyYAML>=6.0
opencv-python>=4.10        # sentetik gösterge çizimi + görüntü işleme
pillow>=10.0
matplotlib>=3.8            # hata grafikleri, rapor figürleri
pytest>=8.0

# İP5 (tespit) — venv Python 3.13
# torch tekerleği PyPI'dan DEĞİL, ayrı dizinden gelir. Bu makinede RTX 4050
# (6 GB, compute 8.9) olduğu için CUDA 12.6 sürümü kuruldu:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# GPU'suz bir makinede kurarken cu126 yerine cpu yazılır. Yanlış tekerlek
# sessizce çalışır, sadece kart kullanılmaz: `torch.cuda.is_available()` ile
# doğrula, `scripts/egit_ip5.py` cihazı ölçüm dosyasına yazar.
torch>=2.0
torchvision>=0.15
ultralytics>=8.3           # YOLOv8 gösterge tespiti

# İP1/İP5 — açık veri setlerinin indirilmesi ve okunması
gdown>=6.0                 # Google Drive (A1/A2 için denendi, klasör 404 verdi)
pyarrow>=15.0              # Hugging Face parquet setleri

# İP10 (MQTT yayını)
# paho-mqtt>=2.1

# İP11 (dijital / 7-segment OCR)
# paddleocr>=2.9
# pytesseract>=0.3

# Gömülü hedefe taşıma (Orange Pi 5 / NPU)
# Tespit modeli ONNX'e aktarılır; Hailo (.hef) ve RKNN (.rknn) derleyicilerinin
# girdisi budur. `scripts/disa_aktar_onnx.py` aktarımı yapar ve çıktının
# PyTorch ile aynı kutuları verdiğini ölçer.
#
# ⚠ KART ÜSTÜNDE torch/ultralytics GEREKMEZ. `src/gauge_vision/` saf
# numpy+opencv+PyYAML'dır; kartta yalnız onnxruntime (ya da Hailo/RKNN çalışma
# zamanı) + opencv + numpy + PyYAML kurulur.
onnx>=1.16
onnxruntime>=1.18
